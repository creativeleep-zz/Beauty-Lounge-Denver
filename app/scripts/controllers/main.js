'use strict';

/**
 * @ngdoc function
 * @name blwebApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the blwebApp
 */

angular.module('blwebApp').controller('MainCtrl', ['$scope', function ($scope) {

   $scope.load = function() {

   };

   //don't forget to call the load function
   $scope.load();
}]);
